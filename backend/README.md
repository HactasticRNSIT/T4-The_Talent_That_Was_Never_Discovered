# HiddenSpark Auth Backend

Run from this folder:

```bash
npm install
npm start
```

The API serves the frontend from `../frontend` and stores users in `data/users.json`.

Create a `.env` from `.env.example` before production use and replace `JWT_SECRET` with a long random value.
